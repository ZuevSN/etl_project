name: 'etl_project'
version: '1.0.0'
config-version: 2

profile: 'etl_project'

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  etl_project:
    staging:
      +materialized: table